<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>hi</title>
</head>
<body>

    <div class="main w-full min-h-screen bg-red-500">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block w-full px-5 py-2 bg-zinc-800 outline-none rounded-lg text-[#ffff]" type="text" placeholder="Title" name="title">
                <textarea class="block  w-full px-5 py-2 resize-none bg-zinc-800 mt-5 outline-none rounded-lg text-[#ffff] " placeholder="write your task" name="details"></textarea>
                <input class="bg-blue-600 p-2 text-[#ffff] rounded-lg mt-2" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-4 flex-wrap">
            <%if(files.length >= 0){ %>
                <% files.forEach(function(val){ %>
                <div class="task w-72 px-3 py-4 rounded-lg bg-yellow-500">
                <h1 class="text-white text-2xl tracking-tighter"><%= val %></h1>
                <a href="" class="text-blue-800 inline-block mt-3">Read more.</a>
            </div>
                <%}) %>
            <% } else{%>
                <h1 class="text-gray-500">not task yet</h1>
           <% }%>
            
        </div>
    </div>

</body>
</html>